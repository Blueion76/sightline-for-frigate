{
  "name": "sightline-for-frigate",
  "version": "1.0.0",
  "description": "Sightline for Frigate \u2014 a responsive Frigate NVR dashboard card for Home Assistant",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node tests/smoke.mjs",
    "verify": "npm run build && npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
