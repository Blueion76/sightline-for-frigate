{
  "name": "sightline-for-frigate",
  "version": "1.1.4",
  "description": "Sightline for Frigate — a responsive Frigate NVR dashboard card for Home Assistant",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node tests/smoke.mjs && node tests/multiview.mjs && node tests/timeline-ux.mjs && node tests/media-browser.mjs && node tests/date-time-filter.mjs && node tests/responsive-layout.mjs",
    "verify": "npm run build && npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
